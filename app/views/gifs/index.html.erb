<%= link_to 'Add a Gif!', new_gif_path %>

<div id="columns">
  
  <figure>
  <% @gifs.each do |gif| %>
    <%= link_to (image_tag gif.url), gif_path(gif) %>
    <figcaption><%= gif.name %></figcaption>
  <% end %>
  </figure>
</div>